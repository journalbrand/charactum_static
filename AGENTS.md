Ignore the .cursorrules file.
Use `sed` for editing files; `nano` is not installed in the environment.

Read all documentation before beginning, including `ontorum/README.md` and all content within `ontorum/docs`. When modifying code, consult any existing documentation related to that part of the project.

Always create or update documentation in the `docs/` directory when the code changes. Document new behavior, features and usage patterns introduced by your changes.

Be sure to run pytest for all changes to the backend, and run npm test with jest for all changes to the frontend.
